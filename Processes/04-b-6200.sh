#Изведете имената на потребителите,
#които не са логнати в момента, но имат живи процеси

comm -23 <( ps -eo user= | sort | uniq ) <( who | awk '{print $1}' | sort  | uniq)

обяснение:
Временно записваме изхода от ps -eo user= | sort | uniq в невидим файл A.

Записваме изхода от who | awk '{print $1}' | sort | uniq в невидим файл B.

Подаваме тези два "файла" на comm.
