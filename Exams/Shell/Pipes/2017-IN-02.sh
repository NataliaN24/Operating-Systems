#Напишете серия от команди, които изтриват:
#а) всички файлове в текущата директория и нейните поддиректории, 
които са с нулева дължина.

$ find . -type f -size 0 -delete

#or

$find . -type -f -empty -delete

#б) 5-е най-големи файла в home директорията на текущия потребител и
нейните поддиректории.

find ~ -type f -printf '%s %p\n' 2>/dev/null | sort -nr | head -n 5 | cut -d ' ' -f2 | xargs rm -f
